@model IEnumerable<OnlineSchool.Models.TEACHER_ROUTINE>

@{
    ViewBag.Title = "Index";
}

<h2>Teachers Routine Table</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TeacherRoutineDirectory)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ADMIN.AdminName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TEACHER.TeacherName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TeacherRoutineDirectory)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ADMIN.AdminName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TEACHER.TeacherName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TeacherRoutine_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TeacherRoutine_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TeacherRoutine_ID })
        </td>
    </tr>
}

</table>
